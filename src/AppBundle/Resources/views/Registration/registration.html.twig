{% extends 'base.html.twig' %}
{% block content %}
	{{ form_start(form) }}
	{{ form_errors(form) }}
	{{ form_widget(form) }}
	<input type="submit" value="{% trans from 'User' %}registration.submit{% endtrans %}" >
	{{ form_end(form) }}
{% endblock %}
